# int i = 10
# int x = 1
# do {
#   x *= 2
#   i -= 1
# } (while i != 0);

    CONST 10
    STORE i
    CONST 1
    STORE x
LOOP_0:
    LOAD x
    CONST 2
    MUL
    STORE x
    LOAD i
    CONST 1
    SUB
    STORE i
    LOAD i
    JNZ LOOP_0